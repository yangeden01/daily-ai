# Daily AI v0.1 Alpha

Mobile-first React + TypeScript + Vite PWA app skeleton.

## 開發

```bash
pnpm install
pnpm dev
```

## 正式建置

```bash
pnpm build
pnpm preview
```

## 自動化測試

```bash
pnpm test
```

測試涵蓋 IndexedDB Repository CRUD，以及 `Daily.xlsx` 備份匯出／匯入。

PWA 安裝功能需要在 `localhost` 或 HTTPS 環境中使用。
